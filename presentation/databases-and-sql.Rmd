---
title: "Databases and SQL"
author: "Ian Dennis Miller"
date: "November 3, 2016"
output:
  ioslides_presentation:
    autosize: yes
    widescreen: true
    css: assets/styles.css
    self_contained: false
---

# Data, Information, Knowledge, Wisdom

<p></p>

## Information | Subtitle

## First Slide {.side-image}

For more details on authoring R presentations please visit <https://support.rstudio.com/hc/en-us/articles/200486468>.

![picture of spaghetti](assets/spaghetti.jpg)

- Bullet 1
- Bullet 2
- Bullet 3

# HTML5 localStorage

## Load the helpers

```{javascript, eval = FALSE}
function load(src) {
  var js = document.createElement('script');
  js.src = src;
  document.getElementsByTagName('head')[0].appendChild(js);
}
```

```{javascript, eval = FALSE}
load("https://iandennismiller.github.io/assets/helpers.js");
```

## Slide With Code

```{javascript, eval = FALSE}
download_json("http://www.gh-impact.com/data/json/b3.json");
// wait
Object.keys(df);
df["mattn"]["s"];
// Array.proto.map, Array.proto.reduce
// the scores come from the data frame keys using this map function
var scores = Object.keys(df).map( function(key) { return( df[key]["s"] ) } );
var average = scores.reduce( function(sum, val) { return( sum + val ) }, initialValue=0 ) / scores.length;
Math.max.apply(Math, scores);
```

Here we go...

## Slide With Code

```{r}
summary(cars)
```
